## What this changes

Briefly describe the change and why.

Fixes #<!-- issue number, if any -->

## How it was tested

- [ ] `ctest --test-dir build --output-on-failure` passes
- [ ] `bash package.sh` builds the plugin archive without QML errors (if QML changed)
- [ ] Verified in a live Plasma session — describe setup (Wayland/X11, monitors):

## Checklist

- [ ] Change is focused on one concern
- [ ] License split respected (MIT vs GPL by directory — see CONTRIBUTING.md)
- [ ] No `build/` or `dist/` committed
</content>
